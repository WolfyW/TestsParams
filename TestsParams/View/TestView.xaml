<UserControl x:Class="TestsParams.View.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestsParams.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="15"/>
        </Style>
    </UserControl.Resources>


    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel.ToolTip>
                <ToolTip>
                    <TextBox Text="{Binding Note}"/>
                </ToolTip>
            </StackPanel.ToolTip>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Название" Margin="0,0,0,0" Height="25"/>
                <TextBox Text="{Binding Path=BlockName}" Margin="5,0,0,0" Width="150" Height="20"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
